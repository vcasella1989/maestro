package:
    package_name: nginx
    ensure: install

  service:
    service_name: nginx
    ensure: running
  
  package:
    packagename: php
    action: install

  service:
    servicename: php
    ensure: running